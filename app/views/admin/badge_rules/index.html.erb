<h1>Все правила</h1>

<p><%= link_to 'Новое правило', new_admin_badge_rule_path %></p>

<%= render 'shared/errors', resource: @badge_rule %>

<table class="table">
  <tr>
    <th>Name</th>
    <th>Author</th>
    <th colspan="3">Действия</th>
  </tr>
  <%= render @badge_rules %>
</table>

<%= link_to 'Назад', admin_badges_path %>
